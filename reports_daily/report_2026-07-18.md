# Daily Trading Firm Report — 2026-07-18 (baseline, book age ~5 h)

Pulled 05:30 CEST from VPS 69.48.202.100 (`trading-firm`, active since 03:08 UTC).
Book reset 2026-07-18 00:15 UTC — this is day 1, so most stats are still at zero.

## 1. Account
| Metric | Value |
|---|---|
| Equity | $9,998.88 (initial $10,000, −0.01 %) |
| Cash | $9,734.39 |
| Exposure | 2.65 % (scout cap 10 %) |
| Open positions | 3 |
| Closed trades | 0 |
| Win rate | n/a (no closed trades yet) |
| Day PnL | −0.01 % (target +0.5…3 %) |

## 2. Strategies
- 174 registered, **0 with live stats yet** (`strategy_stats` empty — no closed trades).
- Signals are flowing: last analyst scan found 14 opportunities across 17 symbols;
  debate candidates cite PA-Pin Bar, PA-Double Top/Bot, Breakout-Swing Failure Fade,
  Turtle Soup, Classic-VPT, ict_pa_ifvg — i.e. the new strategy packs ARE firing.
- Inactive-so-far: everything else (expected 5 h in; real inactivity flags start tomorrow).

## 3. Trades & loss forensics
- **0 closed trades** — no winners, no losers yet. Forensics start when the first
  trades close (daily job will analyze entry / SL / TP quality per loser).

## 4. Open positions (ages ~1 h)
| Symbol | Entry | Now | PnL | SL dist | TP dist | Comment |
|---|---|---|---|---|---|---|
| UNI/USD | 3.5718 | 3.54 | −0.89 % | −1.15 % | +1.51 % | Worst; SL geometry OK, watch |
| BTC/USD | 63,971 | 63,912 | −0.09 % | −1.25 % | +1.08 % | Normal noise |
| AVAX/USD | 6.6333 | 6.63 | −0.05 % | −1.05 % | +1.20 % | Normal noise |

All three have R:R ≈ 1.0–1.2 — thin. If these close red, expect "TP too tight vs
fees/slippage" to be the root cause; the scout TP floor already covers ~1.5× fees
but not slippage spikes. Candidate fix if pattern repeats: raise scout TP floor to
max($0.10, 2× round-trip fees + slippage buffer).

## 5. Agent performance
- Cycle length ~2 min; all agents run each cycle (health, sentiment, regime, analyst,
  risk, sizer, portfolio, debate, compliance, execution, trader, auditor).
- Regime: `risk_off`, BTC 63.9k vs SMA200 73.3k → SCOUT mode active, deploy ≤ 10 % ✓
- Sentiment: neutral, breadth 29 %, F&G 25 (fear) ✓
- Debate (last 3 rounds): 3 candidates each → **APPROVE 0, DOWNGRADE 4, REJECT 5**.
  The brain is genuinely filtering — nothing passes on autopilot. Downgrades cite thin
  track record + volatile regime; rejects cite bad geometry/fee burden.
- LLM vs engine: **stepfun free endpoint is failing ~100 % of calls** right now
  (provider errors + read timeouts), so the deterministic engine carried all 9 debates.
  Degradation path works exactly as designed — no blind pass-throughs.
- Funnel: 14 opportunities → 3 portfolio candidates → debate → 12 compliance rejects →
  0–few executions. Tight but functioning; scout caps are doing their job.

## 6. Errors & delays
- **0 crashes/exceptions in 24 h** (journalctl clean).
- LLM failures: every debate round burns up to **90 s** waiting on the dead/overloaded
  free endpoint before the engine takes over — that's 75 % of each 2-min cycle wasted.
- No exchange/API failures; no restarts besides the 03:08 UTC deploy.

## 7. Missing / to fix (priority order)
1. **LLM circuit breaker for the fallback model** — after N consecutive stepfun failures,
   skip LLM calls for ~30 min instead of paying a 90 s timeout every cycle. Biggest
   cheap win: cycles get ~45 % faster and the engine verdicts are identical.
2. **0 closed trades after 5 h** — fine so far, but if day 2 still shows ~0 closures,
   review scout TP floor / trailing logic so trades actually complete (no data = no
   strategy filtering at week's end).
3. **No /api/strategies or /api/agents endpoints** — daily report needs SSH for those;
   consider adding read-only endpoints (nice-to-have, not blocking).
4. **Paid Hermes still dead** (no credits) — optional: top up to restore full LLM
   debates; the engine covers us meanwhile.

Next report: tomorrow 07:47 CEST (automated).
